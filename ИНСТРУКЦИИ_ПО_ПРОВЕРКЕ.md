# Инструкции по проверке исправления метаданных токенов

## Что было сделано

Исправлена проблема с отображением метаданных токенов на Solscan. Основное изменение:

**Файл `src/metadata-addition.service.ts`:**
- Изменён импорт `createUmi` с `@metaplex-foundation/umi` на `@metaplex-foundation/umi-bundle-defaults`
- Упрощена инициализация Umi SDK в соответствии с официальной документацией Metaplex

Это исправление обеспечивает правильное создание метаданных, которые будут отображаться на Solscan.

## Как проверить, что исправление работает

### Вариант 1: Автоматический тест (рекомендуется)

1. **Запустите сервер:**
```bash
npm run build
npm start
```

2. **В другом терминале запустите скрипт:**
```bash
./create-test-token.sh
```

Скрипт автоматически:
- Проверит баланс кошелька
- Создаст логотип токена
- Загрузит логотип на IPFS
- Создаст и загрузит метаданные
- Создаст токен на Solana
- Выдаст ссылку на Solscan для проверки

### Вариант 2: Прямое взаимодействие с блокчейном

```bash
node create-test-token-cli.mjs
```

Этот скрипт работает без запущенного сервера и напрямую взаимодействует с Solana.

### Вариант 3: Ручное создание токена

Следуйте пошаговой инструкции в файле `РУКОВОДСТВО_ПО_СОЗДАНИЮ_ТОКЕНА.md`

## Что проверять на Solscan

После создания токена откройте ссылку на Solscan и убедитесь, что отображаются:

✅ Название токена  
✅ Символ токена  
✅ Логотип/изображение токена  
✅ Описание  
✅ Атрибуты  
✅ Количество и decimals  

## Информация о сервисном кошельке

- **Адрес:** ESnpcCfEzTu27zimt7buatKXU3ogihyqVozfWJKgv2Jx
- **Сеть:** Solana Devnet
- **Минимальный баланс:** 0.01 SOL

### Как пополнить кошелёк:

```bash
solana airdrop 1 ESnpcCfEzTu27zimt7buatKXU3ogihyqVozfWJKgv2Jx --url devnet
```

Или посетите: https://faucet.solana.com/

## Если возникли проблемы

### Ошибка "Failed to check balance" или "Network connectivity issues"
- Убедитесь, что у вас есть доступ к интернету
- Проверьте, что Solana devnet работает
- Попробуйте через несколько минут

### Метаданные не отображаются на Solscan
- Подождите 1-2 минуты - Solscan нужно время на индексацию
- Обновите страницу в браузере
- Убедитесь, что URI метаданных доступен

### Недостаточно средств (Insufficient balance)
- Пополните кошелёк командой выше
- Проверьте баланс: `curl http://localhost:3000/api/balance`

## Документация

- **FIX_SUMMARY.md** - Полная информация об исправлении (English)
- **РУКОВОДСТВО_ПО_СОЗДАНИЮ_ТОКЕНА.md** - Подробное руководство (Русский)
- **TOKEN_METADATA_FIX.md** - Оригинальная документация (English)

## Примечание о тестировании

В CI/CD окружении отсутствует доступ к сети, поэтому я не смог создать тестовый токен автоматически. 

**Вам нужно запустить один из предоставленных скриптов в окружении с доступом к интернету:**
- Локально на вашем компьютере
- На сервере с доступом к Solana devnet
- В любом окружении с интернет-соединением

После запуска скрипта вы получите ссылку на Solscan, где можно будет проверить, что все метаданные отображаются корректно.

## Техническая информация

**Что изменилось:**
```typescript
// Было
import { createUmi } from "@metaplex-foundation/umi";
const umi = createUmi();
umi.use(defaultPlugins(connection.rpcEndpoint));

// Стало
import { createUmi } from "@metaplex-foundation/umi-bundle-defaults";
const umi = createUmi(connection.rpcEndpoint);
```

**Почему это важно:**
- `createUmi` из `umi-bundle-defaults` автоматически подключает все необходимые плагины
- Это соответствует официальной документации Metaplex
- Гарантирует правильное создание метаданных

**Ссылка на документацию:**
https://developers.metaplex.com/guides/javascript/how-to-create-a-solana-token

## Контакты и поддержка

Если у вас возникли вопросы:
1. Проверьте файлы документации
2. Убедитесь, что следовали инструкциям
3. Проверьте логи сервера на наличие ошибок

---

**Успешного тестирования! 🚀**
