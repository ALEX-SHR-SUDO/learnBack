# Проверка Backend и Структурное Отображение Метаданных

## Что было сделано

В ответ на ваш запрос о проверке бэкенда и структурном отображении движения метаданных, которые записывает клиент, были созданы следующие компоненты:

## 1. Полная Документация Потока Метаданных

**Файл:** `METADATA_FLOW_DOCUMENTATION.md`

Этот документ содержит:

### Структурная Схема Потока
```
КЛИЕНТ → /api/generate-metadata → Загрузка изображения → Создание JSON → IPFS
                                                                           ↓
КЛИЕНТ → /api/create-token → Валидация URI → Создание токена on-chain → Метаданные
                                                                           ↓
                                                            Индексация Solscan
```

### Детальное Объяснение

**Шаг 1: Генерация Метаданных** (`/api/generate-metadata`)
- Клиент отправляет: файл логотипа, имя, символ, описание
- Backend:
  1. Загружает изображение на IPFS → получает imageUri
  2. Создает JSON структуру Metaplex:
     ```json
     {
       "name": "Название токена",
       "symbol": "СИМВОЛ",
       "image": "https://gateway.pinata.cloud/ipfs/...",
       "properties": {
         "category": "fungible"  ← КРИТИЧНО для Solscan!
       }
     }
     ```
  3. Загружает JSON на IPFS → получает metadataUri
- Возвращает: metadataUri, imageUri, sessionId

**Шаг 2: Создание Токена** (`/api/create-token`)
- Клиент отправляет: name, symbol, uri, supply, decimals
- Backend:
  1. Валидирует доступность metadataUri
  2. Проверяет структуру JSON
  3. Создает токен on-chain с помощью `createAndMint()`:
     - Создается mint аккаунт
     - Создается metadata аккаунт (on-chain) ← Это видит Explorer
     - Минтятся токены
  4. Metadata аккаунт содержит ссылку на metadataUri
- Возвращает: mintAddress, ссылки на Solscan

**Шаг 3: Индексация**
- Solana Explorer: читает metadata аккаунт напрямую ✅
- Solscan: индексирует + загружает JSON по URI ⏳ (1-5 минут)

### Почему Метаданные Видны на Explorer, но Не на Solscan

1. **URI недоступен** - IPFS шлюз не отвечает
2. **Неправильная структура JSON** - отсутствуют обязательные поля
3. **Неправильный category** - должен быть "fungible", а не "image"
4. **Задержка индексации** - Solscan индексирует медленнее

## 2. Система Отслеживания Потока Метаданных

**Файл:** `src/metadata-flow-tracker.ts`

Новый модуль, который:
- Отслеживает каждый шаг от генерации метаданных до создания токена
- Логирует детальную информацию с временными метками
- Валидирует структуру метаданных
- Проверяет доступность URI
- Генерирует отчет с рекомендациями

### Интеграция в Существующий Код

**Обновленные файлы:**
1. `src/metadata-generator.service.ts` - добавлено отслеживание генерации
2. `src/metadata-generator.route.ts` - возвращает sessionId
3. `src/metadata-addition.controller.ts` - отслеживание создания токена
4. `src/metadata-addition.service.ts` - отслеживание on-chain операций

## 3. Руководство по Использованию

**Файл:** `METADATA_FLOW_TRACKING_USAGE.md`

Содержит:
- Примеры использования на bash и TypeScript
- Описание новых полей API
- Объяснение вывода в консоль
- Сценарии диагностики проблем

## 4. Тестовый Скрипт

**Файл:** `test-metadata-flow-tracking.sh`

Демонстрирует:
- Генерацию метаданных с получением sessionId
- Использование sessionId при создании токена
- Вывод полного отчета о потоке

## Как Использовать

### 1. Генерация метаданных
```bash
curl -X POST http://localhost:3000/api/generate-metadata \
  -F "file=@logo.png" \
  -F "name=Мой Токен" \
  -F "symbol=MTK" \
  -F "description=Описание"
```

**Ответ:**
```json
{
  "metadataUri": "https://gateway.pinata.cloud/ipfs/...",
  "imageUri": "https://gateway.pinata.cloud/ipfs/...",
  "sessionId": "metadata-flow-1699876543210-abc123"
}
```

### 2. Создание токена с отслеживанием
```bash
curl -X POST http://localhost:3000/api/create-token \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Мой Токен",
    "symbol": "MTK",
    "uri": "https://gateway.pinata.cloud/ipfs/...",
    "supply": "1000000",
    "decimals": "9",
    "sessionId": "metadata-flow-1699876543210-abc123"
  }'
```

**Ответ включает:**
```json
{
  "mintAddress": "E6BSP6vd...",
  "transactionSignature": "5J8...",
  "solscanTokenLink": "https://solscan.io/token/...",
  "sessionId": "metadata-flow-1699876543210-abc123",
  "metadataFlowSummary": "... детальный отчет ..."
}
```

## Консольный Вывод

Теперь в консоли сервера выводится детальная информация:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔍 METADATA FLOW TRACKING STARTED
Session ID: metadata-flow-1699876543210-abc123
Start Time: 2024-11-05T15:30:00.000Z
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ [+0ms] Metadata Generation Request Received
✅ [+523ms] Image Uploaded to IPFS
✅ [+645ms] Metadata JSON Created
✅ [+1234ms] Metadata JSON Uploaded to IPFS

✅ [+0ms] Token Creation Request Received
✅ [+150ms] Validating Metadata URI
✅ [+890ms] Metadata URI Accessible
✅ [+920ms] Metadata Structure Valid
✅ [+1250ms] Starting On-Chain Token Creation
✅ [+5678ms] On-Chain Token Created
✅ [+5690ms] Metadata Account Created On-Chain

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 METADATA FLOW TRACKING COMPLETED
Duration: 6789ms
Summary:
  Total Steps: 10
  ✅ Success: 10
  ⚠️  Warnings: 0
  ❌ Errors: 0
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## Диагностика Проблем

Если метаданные не отображаются на Solscan:

1. **Проверьте отчет потока** в ответе API
2. **Найдите предупреждения** о недоступности URI или структуре
3. **Проверьте metadataUri** - откройте в браузере
4. **Проверьте imageUri** - откройте в браузере
5. **Подождите 2-5 минут** для индексации Solscan
6. **Просмотрите логи** на сервере

## Структура Данных On-Chain

Метаданные записываются в blockchain в двух местах:

### 1. Metadata Account (On-Chain)
```javascript
{
  key: 4,                           // MetadataV1
  updateAuthority: "3BhAs81...",
  mint: "E6BSP6vd...",
  data: {
    name: "Мой Токен",
    symbol: "MTK",
    uri: "https://gateway.pinata.cloud/ipfs/...",  ← Ссылка на JSON
    sellerFeeBasisPoints: 0,
    creators: null
  },
  tokenStandard: 2,                 // Fungible
  isMutable: true
}
```

### 2. Off-Chain JSON (IPFS)
```json
{
  "name": "Мой Токен",
  "symbol": "MTK",
  "description": "Описание",
  "image": "https://gateway.pinata.cloud/ipfs/...",
  "properties": {
    "category": "fungible",         ← Важно для Solscan!
    "files": [
      {
        "uri": "https://gateway.pinata.cloud/ipfs/...",
        "type": "image/png"
      }
    ]
  }
}
```

## Различия: Solana Explorer vs Solscan

| Аспект | Solana Explorer | Solscan |
|--------|-----------------|---------|
| Источник данных | Напрямую из RPC | Индексирует + загружает JSON |
| Скорость | Мгновенно | 1-5 минут задержка |
| Отображение | On-chain metadata | Off-chain JSON по URI |
| Требования | Только on-chain | Валидный JSON + правильный формат |

## Рекомендации

1. **Всегда используйте** `/api/generate-metadata` - гарантирует правильный формат
2. **Сохраняйте sessionId** для связи этапов
3. **Проверяйте отчет потока** после создания токена
4. **Адресуйте предупреждения** до деплоя токена
5. **Тестируйте доступность URI** перед созданием токена
6. **Ждите 2-5 минут** для индексации Solscan

## Запуск Теста

```bash
./test-metadata-flow-tracking.sh
```

Этот скрипт:
1. Генерирует метаданные
2. Проверяет доступность
3. Создает токен с отслеживанием
4. Выводит полный отчет

## Итоги

Теперь вы можете:

✅ **Видеть структурно**, как метаданные проходят через систему
✅ **Отслеживать каждый шаг** от загрузки изображения до индексации
✅ **Диагностировать проблемы** с помощью детальных отчетов
✅ **Понимать разницу** между Explorer и Solscan
✅ **Проверять метаданные** до создания токена
✅ **Получать рекомендации** по исправлению проблем

Все метаданные правильно записываются на blockchain. Если Solscan не показывает их, проблема либо в доступности URI, либо в структуре JSON, либо просто нужно подождать индексацию.
